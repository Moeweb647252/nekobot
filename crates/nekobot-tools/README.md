# Nekobot tools middleware

This crate provides a middleware for Nekobot that allows agents to execute various tools. It enables agents to perform actions such as making HTTP requests, executing shell commands, and interacting with the file system.
